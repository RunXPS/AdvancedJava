import java.util.regex.*;
import java.util.*;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

public class RegExKC {
    /*
     * Pattern pattern = Pattern.compile("(a)(b)*");
     * Matcher matcher = pattern.matcher(input);
     * return matcher.matches();
     */
}